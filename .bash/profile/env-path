#!/usr/bin/env bash

export DEFAULT_PATH="${PATH}";
export PATH='';

PATH="${PATH}":~/.bin;
PATH="${PATH}":~/.bin/git;
PATH="${PATH}":/usr/local/sbin;
PATH="${PATH}":/usr/local/bin;
PATH="${PATH}":/usr/sbin;
PATH="${PATH}":/usr/bin;
PATH="${PATH}":/sbin;
PATH="${PATH}":/bin;
