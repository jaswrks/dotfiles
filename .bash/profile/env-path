#!/usr/bin/env bash

export PATH='';

PATH="${PATH}":~/.bin;
PATH="${PATH}":/usr/local/sbin;
PATH="${PATH}":/usr/local/bin;
PATH="${PATH}":/usr/sbin;
PATH="${PATH}":/usr/bin;
PATH="${PATH}":/sbin;
PATH="${PATH}":/bin;
